export class Notes {
    title: string;
    description : string;
    time: string

    constructor(title: string, description: string, time: string) {
        this.title = title,
        this.description = description,
        this.time = time
    }
}